apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "patentsearchsvc.fullname" . }}-test-connection"
  labels:
    {{- include "patentsearchsvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "patentsearchsvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
